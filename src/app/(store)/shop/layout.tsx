
import { Box } from "@mui/material";

const SubCatLayout = ({ children }: { children: React.ReactNode }) => {
  
  return (
    <Box>
      {children}
    </Box>
  );
};

export default SubCatLayout;
